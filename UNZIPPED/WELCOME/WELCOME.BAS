OPEN "WELCOME.TXT" FOR INPUT AS #1
INPUT #1, C1
INPUT #1, M$
INPUT #1, SPD
INPUT #1, A$
INPUT #1, A$
INPUT #1, A$
CLS
H$ = TIME$
HORA = VAL(LEFT$(H$, 3))
IF HORA > 5 AND HORA < 14 THEN DIA = 1
IF HORA > 12 AND HORA < 22 THEN DIA = 2
A = -2
IF DIA = 1 THEN A$ = "Bom Dia,"
IF DIA = 2 THEN A$ = "Boa Tarde,"
IF DIA = 0 THEN A$ = "Boa Noite,"
A$ = A$ + M$
GOSUB 1000
DIM F(5)
RANDOMIZE TIMER
C = INT(RND * C1)
FOR Z = 1 TO C
INPUT #1, A$
INPUT #1, A$
INPUT #1, A$
INPUT #1, A$
INPUT #1, A$
INPUT #1, A$
NEXT Z
FOR A = 1 TO 5
INPUT #1, A$
GOSUB 1000
NEXT A
CLOSE #1
END

1000 IF UCASE$(LEFT$(A$, 4)) = "PLAY" THEN PLAY RIGHT$(A$, (LEN(A$) - 4)): RETURN
FOR W = 1 TO 6
RESTORE
FOR Y = 1 TO W
READ CLR
NEXT Y
COLOR CLR
LOCATE A + 3, (81 - LEN(A$)) / 2: PRINT A$
FOR Z = 1 TO (10000 - 1000 * SPD): NEXT Z
IF INKEY$ <> "" THEN END
NEXT W
RETURN
DATA 0,8,1,3,7,15

